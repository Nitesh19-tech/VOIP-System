"""
URL configuration for config project.
"""

from django.contrib import admin
from django.urls import include, path

from .views import home


urlpatterns = [

    # =====================================================
    # Root / Health Check
    # =====================================================

    path(
        "",
        home,
        name="home",
    ),

    # =====================================================
    # Django Admin
    # =====================================================

    path(
        "admin/",
        admin.site.urls,
    ),

    # =====================================================
    # Authentication
    # =====================================================

    path(
        "api/auth/",
        include("apps.accounts.urls"),
    ),

    # =====================================================
    # Companies
    # =====================================================

    path(
        "api/companies/",
        include("apps.companies.urls"),
    ),

    # =====================================================
    # Clients
    # =====================================================

    path(
        "api/clients/",
        include("apps.clients.urls"),
    ),

    # =====================================================
    # Number Pool
    # =====================================================

    path(
        "api/numbers/",
        include("apps.number_pool.urls"),
    ),

    # =====================================================
    # SIP Accounts
    # =====================================================

    path(
        "api/sip/",
        include("apps.sip.urls"),
    ),

    # =====================================================
    # Dashboard
    # =====================================================

    path(
        "api/dashboard/",
        include("apps.dashboard.urls"),
    ),

    # =====================================================
    # Call Detail Records
    # =====================================================

    path(
        "api/cdr/",
        include("apps.cdr.urls"),
    ),

    # =====================================================
    # AMI
    # =====================================================

    path(
        "api/ami/",
        include("apps.ami.urls"),
    ),

    # =====================================================
    # Billing
    # =====================================================

    path(
        "api/billing/",
        include("apps.billing.urls"),
    ),

    # =====================================================
    # Trunks
    # =====================================================

    path(
        "api/trunks/",
        include("apps.trunks.urls"),
    ),
    # =====================================================
# Carriers
# =====================================================

    path(
    "api/carriers/",
    include("apps.carriers.urls"),
    ),

# =====================================================
# Routing Plans
# =====================================================

    path(
    "api/routing-plans/",
    include("apps.routing_plans.urls"),
),

# =====================================================
# Routes
# =====================================================

    path(
    "api/routes/",
    include("apps.routes.urls"),
),
]